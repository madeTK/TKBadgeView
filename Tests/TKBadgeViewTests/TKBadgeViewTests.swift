import XCTest
@testable import TKBadgeView

final class TKBadgeViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TKBadgeView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
